{
  "name": "@shield-protocol/relayer",
  "version": "0.2.0",
  "description": "Duelist relayer: submits user-built proofs to Soroban, gasless for the user.",
  "type": "module",
  "main": "./dist/server.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node --env-file=.env dist/server.js",
    "dev": "tsx --env-file=.env src/server.ts"
  },
  "dependencies": {
    "@stellar/stellar-sdk": "^14.6.1",
    "express": "^5.0.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "tsx": "^4.19.0"
  },
  "engines": { "node": ">=20" }
}
